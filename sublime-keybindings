[
	// plantuml
	{ "keys": ["alt+d"], "command": "display_diagrams" },
	
	// markdown edit & markdown preview
	{ "keys": ["alt+m"], "command": "markdown_preview", "args": { "target": "browser"} },

	// close current page
	{ "keys": ["alt+w"], "command": "close" },

	// delete whole line 
	{ "keys": ["ctrl+l"], "command": "run_macro_file", "args": {"file": "res://Packages/Default/Delete Line.sublime-macro"} },

	// close all tabs
	{ "keys": ["alt+a"], "command": "close_all"},

	// Open in browser
	{ "keys": ["ctrl+b"], "command": "open_in_browser"},

	// Add folder to project 
	{ "keys": ["alt+f"], "command": "prompt_add_folder"},

	// Jump back and jump forward 
	{ "keys": ["ctrl+alt+left"], "command": "jump_back" },
	{ "keys": ["ctrl+alt+right"], "command": "jump_forward" },

	// open cmd according current file path
	{ "keys": ["ctrl+c+m"], "command": "cmd" },

	{ "keys": ["ctrl+alt+e"], "command": "open_dir", 
					"args": {"dir": "$file_path", "file": "$file_name"} },
]
